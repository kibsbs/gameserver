const wdfUtils = require("wdf-utils");

const Session = require("wdf-session");

module.exports = {

    name: `getBloomBergs2`,
    description: ``,
    version: `1.0.0`,

    async init(req, res, next) {
        return res.uenc()
    }
}